#!/bin/bash
# this script is a placeholder but it will be for creating the first user from the .env with prompt for password OR with to for the
# user to modify so the user doesn't start with using root and can disable root remote ssh login
